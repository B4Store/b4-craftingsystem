local QBCore = exports[Config.CoreName]:GetCoreObject()


local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(-1570.95, -1039.73, 6.8), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(1729.4, 3329.5, 40.22), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then

    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(-534.56, 5125.97, 90.7), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(-198.31, 3636.51, 63.4), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01`

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(891.95, 2856.21, 56.0), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01`

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(571.89, 149.3, 103.3), true)  

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(947.04, -2527.43, 27.3), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(1714.57, -1597.43, 112.8), true)

local modelHash = `prop_cs_heist_bag_01`



local modelHash = `prop_cs_heist_bag_01` 

if not HasModelLoaded('prop_cs_heist_bag_01') then
    RequestModel('prop_cs_heist_bag_01')

    while not HasModelLoaded('prop_cs_heist_bag_01') do
        Citizen.Wait(1)
    end
end

local obj = CreateObject('prop_cs_heist_bag_01', vector3(785.43, -1487.68, 19.1), true)
